node_modules/
data/runtime/
*.log
.DS_Store

recon/*.png
recon/*.log
recon/e2e-profile/
.vercel
data/prices/

# every-store pipeline (raw archive, manifests, DuckDB) - lives on the runner, never in git
data/pipeline

# manual product-truth audit tool output (scripts/audit-enrich-products.mjs) - working directory, not published
ops/enrichment/
