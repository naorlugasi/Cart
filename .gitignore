node_modules/
data/runtime/
*.log
.DS_Store

recon/*.png
recon/*.log
recon/e2e-profile/
.vercel
data/prices/

# every-store pipeline (raw archive, manifests, DuckDB) - lives on the runner, never in git
data/pipeline
