# מה לעדכן בפרונט (cartFrontend) ובבק (cartBackend) אחרי 19.9.2026

מקור האמת לנתונים: `docs/PIPELINE-CONTRACT.md` (§2.1, §2.2, §4.3). עיצוב התחליפים: `docs/CONCEPTS.md`. מימוש ייחוס: `server/app.js`, `src/pricing/compare.js`, `src/pricing/substitutes.js`, `src/pricing/promotions.js`, `src/pricing/pooling.js` בריפו Cart. **כלל ברזל נשאר**: מחירים ומבצעים רק מהקבצים המפורסמים; אין קריאה לאתרי הרשתות.

## Backend (cartBackend)

### 1. נתונים חדשים בקבצים
- `products.json` גדל מ-4,000 ל-~7,200 (2.8MB). שדות חדשים לכל מוצר: `conceptId`, `size {value, unit, count}`, `privateLabelOf`. מוצרי מותג פרטי נמכרים ברשת אחת בלבד, אז `chains` יכול להיות 1.
- פריטי קטלוג: `promotions[]` בסכמה החדשה (`type: multi|unit|percent|bundleFree|second|discount`, `club`, `clubLabel`, `validTo`, `maxQty`, `promotionId`, `description`), ו-`privateLabel: true` על פריט מותג פרטי.
- `config/concepts/*.json` (326 מושגים): לטעון יחד עם products כדי לקבל `name`/`synonyms`/`sizeUnit` של מושג (לחיפוש ולתצוגת "אותו מושג").

### 2. תמחור: לא לכתוב מחדש, להעתיק את המודולים
`src/pricing/promotions.js` (`priceLine`, `upsellHint`), `src/pricing/pooling.js` (`poolBundles`), `src/pricing/substitutes.js` (`findSubstitute`), ו-`src/pricing/compare.js` (`compareCart`). הם ללא תלויות. הבדלים מהותיים מהגרסה הקודמת:
- מחיר מועדון **לא נכנס לסכום**. `priceLine` מחזיר `total` (לכולם) ו-`club` (רק אם זול יותר).
- `maxQty`: מעבר לו היחידות במחיר מדף. `bundleFree` (2+1), `second` (השני ב-X%).
- איגוד "מגוון": יחידות משורות שונות באותו `promotionId` מקבלות יחד את "N ב-X".
- תחליפים בתוך `compareCart` לפי `substitutes` (ראה 4).

### 3. תשובת `/api/compare` - שדות חדשים
לשורה: `club {lineTotal, promo, savings, label}|null`, `hint {addQty, lineTotal, promo}|null`, `pooled {promotionId, with[]}|undefined`, `substituteReason 'missing'|'cheaper'|null`, `alternative {productId, name, storeItemName, unitPrice, lineTotal, savings, privateLabel, reason}|null`.
לרשת: `club {subtotal, grandTotal, savings, label}|null`, `withAlternatives {subtotal, grandTotal, savings, count}|null`, `substitutedCount`, `savings` (חיסכון ממבצעים בסכום הרגיל).

### 4. הגדרת הלקוח: תחליפים
- בפרופיל המשתמש (Upstash/DB): `substitutes: { policy: 'none'|'privateLabel'|'cheapest', apply: 'ask'|'auto' }`. ברירת מחדל `{ policy: 'privateLabel', apply: 'ask' }` (החלטה 19.9: "לפי הגדרת הלקוח - אוטומטי לזול ביותר או רק באישור").
- `POST /api/compare` מקבל `substitutes` אופציונלי בגוף (גובר על הפרופיל); ערכים לא חוקיים → 400. אנדפוינט לעדכון ההגדרה בפרופיל: `PATCH /api/me` (או המקביל שקיים).
- Handoff: `usedProductId` של שורה שהוחלפה (`status: 'substituted'`) הוא מה שעובר לאתר; `alternative` (גם של פריט חסר) לא עובר עד שהלקוח מאשר וה-UI שולח `substituteProductId` על השורה (מכניזם קיים).
- **תיקון באג 20.9**: `line.substituteProductId` שהלקוח קבע גובר תמיד כשהוא resolve-י ו-inStock ברשת - גם אם המוצר המקורי עצמו זמין שם במחירו (`substituteReason: 'customer'`); אם הוא לא resolve-י נופלים למקורי כשהוא זמין (עם `substituteTried`), אחרת ההתנהגות הקיימת (`missing`/`out_of_stock`).

### 5. חיפוש מוצרים
- לאפשר חיפוש לפי מושג: `synonyms` של המושג + `name`. תוצאות מאותו `conceptId` לקבץ ("חלב 3% - 6 מוצרים ב-9 רשתות"), מותג פרטי מסומן (`privateLabelOf`).
- מוצר מותג פרטי מופיע בתוצאות עם הרשת שלו; בהשוואה הוא יוחלף אוטומטית בשאר הרשתות (סעיף 4).

## Frontend (cartFrontend)

### 1. שורה בסל / במסך ההשוואה
- `promo` (טקסט המבצע) ו-`savings` כמו היום.
- **מחיר מועדון בשורה נפרדת** (החלטה 19.9): אם `line.club` קיים - שורה קטנה מתחת: "למועדון {label}: ₪{club.lineTotal} ({club.promo})". לא להחליף את המחיר הראשי.
- **"קח עוד {hint.addQty} וחסוך"**: אם `line.hint` קיים - כפתור/שורה: "קח {qty+addQty} ב-₪{hint.lineTotal} ({hint.promo})". לחיצה מעדכנת את הכמות.
- **מגוון**: אם `line.pooled` - תג "מבצע משותף עם {pooled.with}".
- **תחליף שהוחל** (`status: 'substituted'`, רק במצב `apply: 'auto'` או אחרי שהלקוח לחץ "החלף"): להציג את `storeItemName` עם "במקום {substituteFor}" ותג: `substituteReason === 'missing'` → "לא נמכר ברשת זו, הוחלף"; `'cheaper'` → "הוחלף בזול יותר".
- **הצעת תחליף** (`line.alternative`, מצב "לשאול אותי" - ברירת המחדל): `alternative.reason === 'missing'` → השורה חסרה (`status: 'missing'`, `lineTotal: 0`) ומתחתיה "לא נמכר ברשת זו. דומה: {alternative.name} ₪{alternative.lineTotal} [החלף]"; `reason === 'cheaper'` → "יש זול יותר: {alternative.name} ₪{alternative.lineTotal}, חיסכון ₪{alternative.savings}" + תג "מותג פרטי" אם `alternative.privateLabel` + **[החלף]**. לחיצה על "החלף" מציבה `substituteProductId = alternative.productId` על השורה ומרעננת השוואה; "בטל" = `substituteProductId: null`.

### 2. כרטיס רשת במסך ההשוואה
- `subtotal`/`grandTotal` כמו היום; `savings` → "מבצעים חסכו ₪X".
- `club` → שורה נפרדת: "עם מועדון {club.label}: ₪{club.grandTotal} (חיסכון ₪{club.savings})". הדירוג לפי הסכום הרגיל בלבד.
- `withAlternatives` → "עם תחליפים: ₪{withAlternatives.grandTotal} (חיסכון ₪{savings}, {count} פריטים)" + כפתור "החל הכל" (מציב `substituteProductId` על כל השורות עם `alternative`).
- `substitutedCount` → "{n} פריטים הוחלפו" ליד כיסוי הסל.

### 3. הגדרות
- מסך הגדרות: "תחליפים ומותג פרטי": (א) מה להציע: "רק מותג פרטי של הרשת" (ברירת מחדל) / "כל מוצר זול יותר" / "לא להציע"; (ב) איך: "לשאול אותי" (ברירת מחדל) / "להחליף אוטומטית". נשמר בפרופיל (Backend §4) ונשלח ב-`substitutes` ל-compare.

### 4. חיפוש ותצוגת מוצר
- תג "מותג פרטי של {chain}" על מוצר עם `privateLabelOf`; `size` להצגה אחידה ("1 ליטר", "6 × 330 מ"ל"; `describeSize` ב-`src/catalog/size.js`).
- קיבוץ תוצאות לפי `conceptId` עם שם המושג; מוצר מותג פרטי שנבחר לסל יקבל בהשוואה תחליף אוטומטי ברשתות האחרות (להסביר בטקסט קטן: "ברשתות אחרות יושווה מוצר דומה").

### 5. מקור ותאריך
ללא שינוי: ליד כל מחיר "לפי מחירון {chain} מ-{generatedAt}" (`priceList`).

## תקלת ייצור 20-21.9 והלקח לחוזה

מחיקת `deliveryCities` מ-`data/chains.json` (בעקבות החלטת 20.9) הפילה את מסך ההשוואה בייצור: כל צרכן שחישב כיסוי מהשדה קרא את היעדרו כ"אף רשת לא מספקת", ובלי סניף מספק גם העמלה יצאה 0. השדה הוחזר (`["*"]`, כניסוח ההחלטה ולא כניחוש), ונקבע כלל: **שדה שפורסם לא נמחק ולא משנה משמעות; שינויים בחוזה הם תוספתיים בלבד, ומחיקה דורשת גרסה ואישור שכל הקוראים הסתגלו.** גם CDN: raw.githubusercontent ענה 304 לבקאנד מול עותק ישן במשך ~30 שעות; הבקאנד שולח עכשיו `Cache-Control: no-cache`.

**cartBackend `df4c5bf` (21.9)**: ממיישם את 20.9 בעצמו (כתובת לעולם לא מסירה רשת; עיר לא מוכרת → סניף ברירת המחדל), ו-`/chains` מחזיר לכל רשת `pickupOnly`, `inStoreOnly`, `deliveryKnown`, `minOrderKnown`. ה-Frontend יכול להציג תג איסוף עצמי ולהבחין בין "תנאים לא ידועים" ל-₪0. `notDelivering` יכיל מעתה רק רשתות `inStoreOnly`.

## דירוג עם או בלי דמי משלוח (הוחלט 20.9)

`POST /api/compare` מקבל `ranking: 'total'|'goods'`; ברירת המחדל `total` שומרת על ההתנהגות הקיימת. התשובה מחזירה `ranking`. **Frontend**: מתג "לכלול דמי משלוח בהשוואה", ולידו הסבר קצר - רשת איסוף עצמי אינה גובה משלוח, ולכן בדירוג לפי סכום כולל היא תנצח גם כשהקנייה עצמה יקרה יותר. סמנו בבירור מנצח שהוא `pickupOnly`. **Backend**: להעביר את הבחירה מהפרופיל או מהבקשה.

## תוספות 20.9: קטגוריות ומוצרי מושג
- **קטגוריות**: המחירונים לא מכילים קטגוריה; היא מחושבת אצלנו (`category` על המוצר). ה-UI מציג לפי `category` בלבד. "כללי" גדול בכוונה (מוצר לא ברור לא נכנס לקטגוריה שגויה). ה-Backend לא צריך לחשב קטגוריות; אם צריך סיווג עדין יותר בחיפוש - לפי `conceptId` ושם המושג.
- **מוצרי מושג** (`kind: 'concept'`, `gtin: null`, `isWeighted: true`, `unit: 'ק"ג'`): מלפפון, עגבניות, בננה, סלמון, פסטרמה... בהשוואה הם מתומחרים לק"ג לפי הפריט השקיל הזול של כל רשת (`matchMethod: 'concept'`, `storeItemName` = שם הפריט של הרשת). כמות = ק"ג (עשרוני). **Frontend**: להציג אותם בקטגוריה עם יחידה "ק"ג", בורר כמות עשרוני (0.5, 1, 1.5), ובמסך ההעברה לעגלה לסמן "לא ניתן להעברה לעגלה עדיין" (כמו כל שקיל). **Backend**: לא לדרוש `gtin` על מוצר; `id` יכול להתחיל ב-`c-`; פריטי קטלוג שקילים נושאים `conceptId`; handoff מדלג על שקילים ומדווח אותם ב-`skipped` עם סיבה `weighted`.

## מהפך המחלקות 20.9: כל מוצר נסקר אחד-אחד

הדיווחים ("מיץ פטל בפירות", "אסם פודינג אינסטנט בחלב וביצים", "155 מתוך 190 בירקות ופירות מעובדים")
לא היו באגים נקודתיים אלא תסמין: הקטגוריה חושבה ממילות מפתח בלבד. **כל 7,231 המוצרים עברו סקירה פרטנית**
מול טקסונומיה כתובה - `docs/CATEGORIES.md` - והתוצאה נשמרת כתווית מפורשת לכל מוצר
(`config/categories/labels.json`, גובר על המושג ועל מילות המפתח).

**מה משתנה בנתונים**: רק שדה `category` (ו-`icon` שנגזר ממנו). אין שינוי במבנה, במזהים או במחירים.
מספרים לפני/אחרי: כללי 1,051 → 187, ניקיון וטואלטיקה 1,221 → 1,671, מעדנייה 250 → 544,
חלב וביצים 691 → 828, ירקות ופירות 187 → 225, בשר ועוף 215 → 128 (נקניקים ומעושנים עברו למעדנייה).

**Frontend - מה כדאי לעשות**
1. שום שינוי קוד לא נדרש; רענון `products.json` מספיק.
2. **תוויות תצוגה**: מפתחות ה-`category` נשארים כמו שהם לנצח, אבל שתיים מהתוויות מטעות את הלקוח.
   מומלץ להציג: `שימורים` → "מזווה ושימורים", `מעדנייה` → "מעדנייה וקפואים", `בשר ועוף` → "בשר, עוף ודגים",
   `כללי` → "שונות". המיפוי המלא בראש `docs/CATEGORIES.md`.
3. קפה, תה וקפסולות עברו ל"משקאות"; חד"פ, נייר וסוללות ל"ניקיון וטואלטיקה"; ירקות קפואים, נקניקיות,
   סלטים מוכנים וטופו ל"מעדנייה"; עוגיות, וופלים וגלידה ל"חטיפים וממתקים"; אבקות (פודינג, ג'לי, מרק)
   ותבלינים ל"שימורים". אם יש אצלכם מיפוי קשיח כלשהו לפי שם מוצר - למחוק אותו, הוא כבר לא נחוץ.

**Backend - מה כדאי לעשות**
1. אם ה-API מחשב קטגוריה בעצמו לפריט כלשהו - להפסיק ולקחת את `category` מהמוצר.
2. מי שטוען את `config/` (כמו המושגים) יכול לטעון גם את `config/categories/labels.json`, אבל אין צורך:
   הקטגוריה כבר מוטמעת בכל מוצר ב-`products.json`.
3. מוצר חדש שיופיע במחירונים לפני שנסקר יקבל קטגוריה ממילות המפתח, ובמקרה גבולי "כללי" - זה תקין
   וזמני עד סבב הסקירה הבא.

## 22.9: מוצרים שקילים - המחיר לק"ג תוקן, וההעברה לעגלה תומכת בהם

**מה קרה.** חמישה מ-45 מוצרי המושג השקילים תומחרו לפי שורה שאינה קילו (צרור כוסברה ב-3.20, מגש פטריות ב-9.90, גזר אורגני,
"סלט מוכן" שהוא מדף). הבנייה דורשת עכשיו `isWeighted` מכל שורה, מוציאה אורגני ודלי, ומדיחה רשת שרחוקה מפי 3 מחציון המושג
(פירוט: `docs/CONCEPTS.md` §6). **45 → 40 מוצרי מושג** מריצת מרלוג הבאה; `chains` על מוצר מושג = הרשתות שהסכימו.
במקביל ה-handoff מקבל פריטים שקילים: כל פריט ב-payload נושא `isWeighted` ו-`unit`, `qty` בק"ג, וה-injector שולח אותו בפורמט
השקיל של כל רשת (שופרסל `BY_WEIGHT`, רמי לוי `amount` בק"ג, Self Point `soldBy: "Weight"`, חצי חינם `Type: 2`, יוחננוף `quantity: 0.5`).
אומת מול העגלות החיות של חמש המשפחות ב-22.9 (`docs/HANDOFF.md`, "מוצרים שקילים").

**Backend - מה לעשות**
1. `POST /api/handoffs`: להפסיק לדלג על שקילים (`skipped.reason: 'weighted'`). שורה שקילה עוברת כמו כל שורה, עם `qty` עשרוני (0.5, 1.5).
2. פריטי ה-handoff (וה-payload ל-injector) מכילים `isWeighted: boolean` ו-`unit: 'ק"ג' | "יח'"`. `qty` של פריט שקיל הוא ק"ג.
3. סוג שגיאה חדש בדיווח ה-injector: `weighted_unsupported` - פריט שקיל ל-adapter בלי נתיב שקיל. לא אמור לקרות ב-14 הרשתות (כולן מכוסות), אבל לסווג כמו `rejected` (הפריט מסומן, שאר הסל נטען), לא כהתראה קריטית.
4. `priceUnsure` / `uncomparable` נשארים - הם מגינים גם מפני שורה שקילה שרשת אחת מפרסמת מוזר; פשוט יהיו נדירים יותר.

**Frontend - מה לעשות**
1. להסיר את "לא ניתן להעברה לעגלה עדיין" ממוצרי המושג ומכל פריט `isWeighted`.
2. בפריטי ההעברה ובדיווח: `× 0.5 ק"ג` לפריט שקיל (השדה `unit` בפריט), `× 2` לפריט רגיל.
3. בורר הכמות לשקילים כבר בצעדי 0.5 - זה תואם לצעד של כל הרשתות שנבדקו (0.5 ק"ג). ה-injector מצמיד לצעד של הרשת אם הוא שונה, והכמות שנשלחה בפועל חוזרת ב-`results[].qty`.
4. חמישה כרטיסים ייעלמו (כוסברה, פטרוזיליה, סלט מוכן ועוד שניים): מוצר שפחות מ-3 רשתות מוכרות במשקל לא מתפרסם - כרטיס חסר עדיף על מחיר שגוי.

## 22.9: `sourceDate` - מאיזה יום המחירים באמת (שבתות וחגים)

**מה השתנה בנתונים.** כל `data/catalogs/<chain>.json` מקבל שדה חדש, `sourceDate`: הרגע שבו הרשת ייצרה את קובץ המחירים, מתוך החותמת בשם הקובץ בפורטל (למשל `2026-09-19T03:40:00+03:00`). `generatedAt` נשאר ומשמעותו לא השתנתה: זמן ההורדה. השדה מופיע מהפרסום הבא של מרלוג; עד אז, ובקטלוג שאין לו חותמת, הוא `null`. שדה חדש בלבד, לפי כלל החוזה.

**למה.** בשבת ובחג הרשתות לא מפרסמות מחירונים. ההורדה לוקחת את הקובץ האחרון שיש (של שישי), ואז `generatedAt` אומר "היום" על מחירים של אתמול. אחרי חג של יומיים-שלושה זה הבדל שהלקוח מרגיש, ואין לו דרך לדעת.

**Backend.** `prices.asOf` ו-`stale` ב-`/health` ובתשובת `/compare` מחושבים היום מ-`generatedAt` (`src/catalog/index.ts`, `dates = ...generatedAt`). לעבור ל-`sourceDate ?? generatedAt` לכל רשת, ולהוסיף ל-`Catalog` ב-`types.ts` את `sourceDate?: string | null`. `stale` נשאר לפי אותו סף, רק מול התאריך הנכון. אין שינוי בצורת התשובה.

**Frontend.** ליד "לפי מחירון <רשת>, חנות <store>, מ-<תאריך>" להציג את `sourceDate` כשהוא קיים, אחרת `generatedAt` (או `asOf` מ-`priceListMeta`, שכבר בוחר). כדאי ניסוח "מחירים נכונים ל-<תאריך>" כשהתאריך אינו היום.

## 22.9: איסוף עצמי עולה כסף - `fulfilment`, `pickupFee`, ותיקון שוק העיר

הפרונט הראה שיוחננוף זוכה בסל של 12 פריטים ב-₪213.90 מול ₪243.30 ברמי לוי רק בזכות `deliveryFee: 0`. נבדק מול התקנונים (22.9):

- **יוחננוף פיקאפ**: אין משלוח בכלל; **דמי שירות ₪15** לכל הזמנת איסוף, אחידים (תקנון האתר 1.9). מינימום הזמנה לא פורסם. בקובץ: `fulfilment: "pickup"`, `deliveryFee: null`, `deliveryKnown: false`, `pickupFee: 15`, `pickupFeeKnown: true`, `minOrder: null`, `minOrderKnown: false`.
- **שוק העיר**: הייתה מסומנת `pickupOnly` בטעות; היא שירות משלוחים ("עד הבית") עם איסוף עצמי אופציונלי. דמי משלוח לפי אזור וחלון (לא נקוב), מינימום ₪250, איסוף: דמי טיפול ₪15 לפחות. `pickupOnly: false`, `fulfilment: "delivery"`, `pickupAvailable: true`, `pickupFee: 15`.
- כל שאר הסניפים קיבלו `fulfilment: "delivery"` (אושר עד: `"inStore"`). שדות חדשים בלבד; `deliveryFee` של נקודת איסוף השתנה מ-0 ל-`null`, כי 0 היה טענה שאף מקור לא תמך בה.

**Backend** (`src/compare/geo.ts`, cartBackend): `delivery` בתשובת `/chains` הוא מעכשיו **דמי הערוץ** (`pickupFee` לנקודת איסוף, `deliveryFee` לסניף שמספק), `deliveryKnown` מתייחס לאותם דמים, ונוסף `feeType: "delivery" | "pickup"`. השם אינו מקוצר יותר: `"יוחננוף פיקאפ"` מגיע כפי שהוא, כי זו הישות שמתומחרת.

**Frontend**: בשורה של נקודת איסוף להציג "דמי איסוף ₪15" ולא "משלוח ₪0"; `feeType` אומר איזה. "הכי זול ביוחננוף" צריך להיקרא "הכי זול ביוחננוף פיקאפ", השם כבר מגיע כך.

**דמים לא ידועים (הוחלט 22.9, נאור):** רשת שדמי המשלוח או דמי האיסוף שלה אינם ידועים (`deliveryKnown: false`) מקבלת **כוכבית אדומה ליד שמה** באתר, גם בכותרת הרשת וגם בשורת ההשוואה, עם הסבר לפי `feeType`: "דמי משלוח נקבעים בקופה" / "דמי איסוף לא פורסמו". **הדירוג לא משתנה**: היא מדורגת לפי המוצרים בלי תוספת, ויכולה להיות "הכי משתלם"; הכוכבית היא הגילוי. אין שינוי במנוע ובבקאנד.

## 22.9: רשת שנפלה עדיין מוצגת - `fetchStatus`/`failedSince` וכוכבית שנייה

**החלטת נאור 22.9** (`docs/PLAN-PER-CHAIN-AND-PRICE-HISTORY.md` §א): רשת שההורדה היומית שלה נכשלה לא נעלמת מההשוואה - היא ממשיכה להיבנות מה-`catalog.full.json` האחרון שהצליח, בלי גבול ימים, כל עוד יש לה קובץ כלשהו על הדיסק. רק רשת בלי שום קובץ (`status: "missing"`) יורדת, כמו היום. הצינור כותב את הסטטוס לכל רשת ב-`data/pipeline-status.json` ומעתיק אותו גם לתוך כל קטלוג דק (`fetchStatus`, `failedSince`, `fetchedAt` - `docs/PIPELINE-CONTRACT.md` §2.2/§2.4).

**Backend - מה לעשות.** להעביר `priceStatus: { status, sourceDate, failedSince }` לכל רשת ב-`/chains` וב-`/compare` (מתוך `fetchStatus`/`sourceDate`/`failedSince` של הקטלוג הדק - `status` הוא `fetchStatus`, לא שם שדה חדש בצינור). זה בנוסף לשדות הקיימים (`generatedAt`, `source.store`), לא במקומם.

**Frontend - מה לעשות.** להשתמש **באותו רכיב כוכבית אדומה** שכבר קיים לדמי משלוח לא ידועים (סעיף למעלה), עם טקסט שונה כש-`priceStatus.status === "failed"`: "אין לנו מחירים עדכניים לרשת זו בגלל תקלה. המחירים מ-`<priceStatus.sourceDate>`". שתי הכוכביות יכולות להופיע יחד על אותה רשת (תקלת הורדה ודמי משלוח לא ידועים הם שני דברים בלתי-תלויים). **הדירוג לא משתנה** - הרשת נשארת במקומה בהשוואה, בדיוק כמו הכוכבית הקיימת.

## 22.9: תחליפים - הגדרה, כללים, משפחות

**מה השתנה.** התחליף עד היום היה "אותו `conceptId` וגודל בטווח" - וזה הציע פילה סלמון קפוא ביחידה במקום סלמון טרי לק"ג, מחית תפוחים במקום תפוחים, בורקס תפוח אדמה במקום בורקס גבינה, גאודה מגוררת במקום פרוסה (מדגם של 528 הצעות: 20 שקיל-מול-יחידה, 19 במחיר של פחות מ-40% מהמקור). ההגדרה עכשיו: תחליף הוא מוצר שהלקוח היה שם באותו מקום במתכון בלי לשים לב לשום דבר חוץ מהמותג והגודל. "אותו מושג" נשאר הכרחי, לא מספיק - עשרה כללים נוספים (צורת מכירה, קטגוריה, גודל, מארז מול יחידה, צורה פיזית, תזונה, נדרש, אחוז, וריאנט, טווח מחיר), כל אחד עם טוקן סירוב משלו, מפורטים ב-`docs/CONCEPTS.md` §4. על מדגם של 614 מוצרים ב-4 רשתות: 1,428 הצעות בהתנהגות הישנה → 651 עכשיו. מושגי-מדף (עוגיות וכו') מקבלים `conceptPolicy: 'missingOnly'` - מוצעים רק לשורה חסרה; מערכות לא-תואמות (קפסולות קפה, חיתולים, יין...) מקבלות `'none'`. מושגים כמו "אורז" (לבן/בסמטי/יסמין/מלא) מוגדרים כ"משפחה" ועומדים זה במקום זה **רק** לשורה חסרה, ורק אחרי שאותו-מושג נכשל. מותג פרטי: בין מועמדים באותו מחיר מנצח מעכשיו המותג הפרטי של הרשת (חדש); הדגל `privateLabel` על כל הצעה ללא שינוי.

**Backend - מה לעשות**
1. להעתיק ליד `substitutes.js` הקיים: `src/pricing/substituteRules.js` (הכללים והחתימות) ואת הגרסאות המעודכנות של `src/pricing/substitutes.js` ו-`src/pricing/compare.js` (`evaluateSubstitutes`/`findSubstitute`/`compareCart`).
2. לטעון `config/substitutes/rules.json` ליד `config/concepts/*.json` - קובץ יחיד, בלי אינדקס (בשונה מ-`config/concepts/index.json`).
3. שדות חדשים: כל הצעה (`alternative`) נושאת גם `tier: 'concept'|'family'` ו-`family: {id,name}|null` (הצעה זולה יותר על שורה קיימת היא תמיד `tier: 'concept'`, `family: null` - היא לא חוצה מושגים). שורה שהוחלפה אוטומטית (`apply: 'auto'`) לשורה חסרה מקבלת גם `substituteTier` ו-`substituteFamily` באותה צורה.
4. `scripts/substitute-audit.mjs` - כלי ביקורת ידני, `--legacy` מריץ את ההתנהגות הישנה להשוואה; אינו חלק מהצינור.

**Frontend - מה לעשות**
1. כש-`alternative.tier === 'family'` להציג "מאותה משפחה: <alternative.family.name>" (למשל "מאותה משפחה: אורז") ליד ההצעה; כש-`tier === 'concept'` אין תוספת.
2. תגית "מותג פרטי" (`alternative.privateLabel`) נשארת כמו שהיא.
3. הניסוח של הסיבה (`reason: 'missing'` מול `'cheaper'`) לא השתנה.

## 23.9 (המשך): מחלקה שלישית - `בית וכלים`

נאור פתח גם `בית וכלים` (23.9, אחרי הצהריים). 70 מוצרים עוברים אליה מ"כללי": סכו"ם, סירים, אחסון, מיטות ומזרנים, תיקים
וצידניות, מברגות וסולם, מנגל וכירות גז, מטענים, כבלים ואוזניות. עוד 24 עברו מ"כללי" לניקיון וטואלטיקה (דליים, מגבים, יעים,
מיכלי פלסטיק, תיקי איפור) ו-7 לתינוקות. "כללי" נשאר עם 16 מוצרי פארם ואופטיקה (משקפי קריאה, מד חום, מד לחץ דם).
**Backend**: עוד שורה ב-`DEPARTMENTS`: `"בית וכלים": { id: "home", label: "בית וכלים", icon: "🏠" }`, ב-`CATEGORY_ORDER` אחרי
`pets` ולפני `general`. **Frontend**: 13 מחלקות; אותן הערות כמו למטה.

## 23.9: שתי מחלקות חדשות - `תינוקות` ו-`בעלי חיים`

נאור החליט (23.9) לפתוח שתי מחלקות, אחרי בדיקה באתרי רמי לוי, יוחננוף וחצי חינם: לשלושתן מחלקה נפרדת לתינוקות
("פארם ותינוקות > מזון לתינוקות", "תינוקות ופעוטות") ולבעלי חיים ("בעלי חיים", "מזון וציוד לבעלי חיים"), ואף אחת לא
שמה תמ"ל בחלב או מזון חתולים ב"שונות". הרשימה המלאה ב-`docs/CATEGORIES.md`.

**מה משתנה בנתונים** (מהפרסום הבא של מרלוג): רק `category` (ו-`icon`). ערכים חדשים: `תינוקות` (~195 מוצרים: 77 מ"כללי"
- תמ"ל, מחיות, דייסות, בקבוקים ומוצצים; ~117 מ"ניקיון וטואלטיקה" - חיתולים, מגבונים לתינוק, רחצה לתינוק, קרם החתלה)
ו-`בעלי חיים` (26 מוצרים, כולם מ"כללי"). "כללי" יורד לכ-100 מוצרים שהם באמת שונות (מטענים, דליים, משקפי קריאה).

**Backend** - כבר עמיד: `src/catalog/categories.ts` נותן למפתח לא מוכר `id: cat-<תעתיק>`, תווית = השם, `icon: null`, ומקום
אחרון ב-`CATEGORY_ORDER`. מומלץ להוסיף שתי שורות ל-`DEPARTMENTS` כדי שה-id יהיה יציב וקריא:
`"תינוקות": { id: "baby", label: "תינוקות", icon: "🍼" }`, `"בעלי חיים": { id: "pets", label: "בעלי חיים", icon: "🐾" }`,
ולמקם אותן ב-`CATEGORY_ORDER` אחרי `household` ולפני `general`. בלי זה ה-URL של המחלקה יהיה `cat-tynvqvt` וייתכן שישתנה
אם התעתיק ישתנה.

**Frontend** - הקטגוריות מגיעות מה-API, כך שהמחלקות יופיעו לבד. לבדוק: (1) שתווית/אייקון חסרים לא שוברים את הטאב
(`icon` יכול להיות `null` עד שהבקאנד יוסיף); (2) גוון רקע לכרטיס אם יש מיפוי לפי `category`; (3) "שונות" קטן עכשיו - אפשר
להשאיר אותו אחרון.

**תחליפים**: `config/concepts/general.json` - המושגים `baby-formula`, `baby-food-puree`, `pacifier` עברו ל-`תינוקות`
ו-`pet-food-dog`/`pet-food-cat` ל-`בעלי חיים`; כלל הקטגוריה בתחליפים ממשיך לעבוד (שני המוצרים באותה מחלקה חדשה).
תמ"ל נשאר ב"לעולם לא" של התחליפים.

**תחליפים - שלוש החלטות של נאור (23.9, `config/substitutes/rules.json`)**: טווח המחיר הוחמר מפי 3 לפי 2; כלל הכשרות
(מהדרין) בוטל; ואין משפחות בכלל. החוזה לא משתנה: `alternative.tier` ו-`substituteTier` נשארים בשדות, אבל יהיו תמיד
`'concept'`, ו-`family`/`substituteFamily` תמיד `null` עד שתוגדר משפחה מחדש. אין מה לשנות בקוד; פחות הצעות לשורה חסרה.

## שאלות פתוחות לנאור
1. מותגי בית של טיב טעם, קשת טעמים, מחסני השוק, שוק העיר (הקבצים לא מסמנים; צריך שם מותג).
2. ~~החלפה אוטומטית של פריט חסר~~ הוחלט 20.9: גם פריט חסר שואל (`alternative.reason: 'missing'`); רק `apply: 'auto'` מחליף לבד.
