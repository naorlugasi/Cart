# TODO - מה עדיין לא עובד (נכון ל-8.9.2026)

מצב נוכחי: 13 רשתות (שופרסל, רמי לוי, קרפור, יוחננוף, חצי חינם, ויקטורי, טיב טעם, יינות ביתן, מחסני השוק, קשת טעמים, קוויק, שוק העיר,
אקספרס מהדרין) מאומתות מול האתרים החיים עבור **עגלת אורח ומוצרים ארוזים** (`recon/e2e-<chain>.json`, `recon/selfpoint-live.json`). הרשימה למטה היא כל מה שנשאר פתוח, לפי סדר עדיפות מוצע.

## 1. מוצרים שקילים (ירקות, פירות, בשר, מעדנייה)

כל ה-adapters שולחים כמות ביחידות בלבד. פריט שקיל יידחה או ייכנס כ-"N יחידות", תלוי ברשת.

- [ ] פלטפורמה: להעביר ב-payload של ה-handoff את `isWeighted` ו-`unit` לכל פריט (הקטלוג כבר מכיל אותם; `payloadFor` ב-`src/handoff/handoffService.js` מסנן אותם).
- [ ] injector: לחשוף את השדות כמשתני תבנית (`isWeighted`, `unit`, `sellingMethod`).
- [ ] שופרסל: `sellingMethod: "BY_WEIGHT"` וכמות בק"ג לפי ה-step של הפריט (נראה בהקלטה: `data-selling-method`, `data-step`, `max-quantity`).
- [ ] רמי לוי: אובייקט המוצר מ-`/api/catalog` מכיל `prop.by_kilo` / `sw_shakil` ו-`multiplication` (צעד, למשל 0.5); ה-`amount` ב-localStorage הוא המשקל.
- [ ] קרפור: `soldBy` בשורת העגלה, `isWeighable` / `unitResolution` במוצר - להקליט הוספה של פריט שקיל.
- [ ] יוחננוף: להקליט הוספה של פריט שקיל (לא ברור באיזו יחידה Magento מבטא משקל שם).
- [ ] לאמת עם `node scripts/e2e-handoff.mjs <chain>` אחרי הוספת פריט שקיל ל-`REAL_ITEMS`.

הערכה: יום-יומיים.

## 2. שופרסל - קודי מוצר

נפתר ברובו 8.9.2026: קוד האתר נגזר מה-ItemCode של קובץ המחירים (ברקודי 729000 מקוצרים, השאר כמו שהם), אומת מול העגלה.

- [ ] חלק מהפריטים המיובאים (ברקודים שאינם 729) עדיין נדחים ע"י האתר; לבדוק אם יש להם קוד פנימי אחר ולבנות id-map מדפי המוצר.
- [ ] לסמן בקטלוג פריטים שנדחו כדי שה-UI יציג "יתווסף ידנית" במקום כשל.

## 3. רמי לוי - משתמש מחובר

ה-adapter ממלא את עגלת האורח ב-localStorage. למשתמש מחובר יש עגלת שרת (`EcomToken`) והסל שלנו לא יופיע בה.

- [ ] להקליט את קריאות העגלה של משתמש מחובר (דורש חשבון אמיתי; לא ניתן לבצע מהסביבה האוטומטית).
- [ ] לזהות מצב מחובר (`localStorage.ramilevy.authuser.user`) ולבחור אסטרטגיה בהתאם.

## 4. סניף / אזור משלוח

כל אתר משתמש בסניף ברירת המחדל שלו (קרפור 3003 כפר סבא, רמי לוי 331, יוחננוף "צומת חולון") עד שהמשתמש בוחר אחר. העגלה נכונה, אבל מחירים וזמינות עשויים להשתנות לפי סניף.

- [ ] להעביר את הסניף שנבחר בפלטפורמה (`branchId` ב-handoff) ל-`vars` של ה-adapter במקום ברירת המחדל.
- [ ] למפות בין הסניפים ב-`data/chains.json` למזהי הסניפים של האתרים.

## 5. קטלוגים אמיתיים

הושלם 8.9.2026 (`npm run prices:fetch` / `prices:online` / `products:build`): 4,000 מוצרים לפי ברקוד, 9 רשתות עם מחירי
אונליין אמיתיים. נשאר:

- [ ] ויקטורי ומחסני השוק: laibcatalog.co.il מחזיר "אין קבצים להצגה" לכל חיפוש (גם לרשת כולה) - לבדוק שוב / לפנות לרשת; בינתיים מחוץ להשוואה.
- [ ] שוק העיר ואקספרס מהדרין: לא נמצא פורטל שקיפות מחירים. אפשרות: מחירים מה-API של האתר (Self Point) דרך דפדפן אמיתי (Cloudflare חוסם אוטומציה).
- [ ] קשת טעמים: סניף האונליין (ממ"ר) לא מפרסם קובץ; משתמשים בסניף 014. לבדוק אם ה-API של האתר נותן מחירי אונליין.
- [ ] רמי לוי: הקובץ הוא של סניף 005, המחירים עצמם מוחלפים במחירי האונליין מה-API; מוצר שה-API לא מחזיר מסומן "לא במלאי".
- [ ] 605 מוצרים בקטגוריה "כללי" - לשפר את חוקי הקטגוריות ב-`scripts/build-products.mjs` (או לקחת קטגוריות מה-API של יוחננוף/חצי חינם).
- [ ] שמות מוצרים: הקבצים מכילים שמות קצוצים (למשל "חלב תנובה 3% שומן 2"); לשקול העדפת שמות מה-API של האתרים.
- [ ] ריצה מתוזמנת (יומית) של הצינור + פריסה, כדי שהמחירים יישארו עדכניים.

## 6. קרפור בבדיקה האוטומטית

Cloudflare חוסם Chromium אוטומטי (גם `channel: 'chrome'`), ולכן `scripts/e2e-handoff.mjs carrefour` עובד רק במצב `--manual`
(פותחים את ה-URL בדפדפן אמיתי עם התוסף). האימות בוצע עם אותו injector מדפדפן אמיתי.

- [ ] להריץ פעם אחת את הזרימה המלאה עם הסימנייה בכרום של משתמש אמיתי ולשמור את הדיווח ב-`recon/e2e-carrefour.json` (וכך גם לשאר רשתות Self Point שמאחורי Cloudflare: ויקטורי, מחסני השוק, קשת טעמים, יינות ביתן, קוויק, שוק העיר, אקספרס מהדרין; טיב טעם נבדקת אוטומטית).
- [ ] סניפי ברירת מחדל ברשתות Self Point: פריט שאינו פעיל בסניף (למשל באקספרס מהדרין) נשמט בשקט ע"י האתר ומדווח כ-rejected; לשקול סינון `branch.isActive` ב-lookup והצעת סניף אחר.
- [ ] נתוני המשלוח של 9 הרשתות החדשות ב-`data/chains.json` הם ברירת מחדל (דמי משלוח, מינימום, ערים) - להשלים.

## 7. ערוץ ללא תוסף (bookmarklet / WebView)

הושלם 8.9.2026: ה-bookmarklet עצמאי (הקוד בסימנייה, הנתונים בכתובת, התוצאה ב-postMessage ובפיקסל תמונה כש-fetch חסום)
ומאומת בכל 4 הרשתות כולל רמי לוי, גם כשהטאב לא נפתח מהפלטפורמה. התוסף נשאר כלי פיתוח בלבד.

- [ ] הסימנייה מכילה את ה-injector; כשהוא משתנה צריך לגרור אותה מחדש. להוסיף גרסה לסימנייה ולהציג בדיאלוג "יש גרסה חדשה" כשהדיווח מגיע מגרסה ישנה.
- [ ] מובייל (Safari iOS / Chrome Android) בלי שורת סימניות נוחה: לבדוק את ה-WebView (`mobile/`) על מכשיר אמיתי מול רשת אמיתית; הסקריפט כבר כולל את ה-payload.
- [ ] ה-payload בכתובת אינו חתום (רק ה-`cart_id`). מי שילחץ על הסימנייה בכתובת מזויפת יוסיף לעגלה שלו את מה שהכתובת מכילה; לשקול חתימה על ה-payload ואימות בסימנייה עם מפתח ציבורי.

## 8. חוסן לאורך זמן

- [ ] הטוקן האנונימי של רמי לוי (`ANONYMOUS_TOKEN` ב-`src/handoff/adapters/ramilevy.js`) מגיע מה-bundle של האתר ועלול להתחלף; להוסיף בדיקה תקופתית (`scripts/probe-adapters.js`) שמזהה 401 ומרימה התראה.
- [ ] `scripts/probe-adapters.js` בודק רק `OPTIONS` על נתיב ה-add; לעדכן אותו למבנה החדש (lookup / session) ולהריץ מ-GitHub Actions (מכתובות ענן חלק מהאתרים חוסמים - ראו `docs/HANDOFF.md`).
- [ ] הודעות ה-banner באתר הרשת: להוסיף קישור חזרה לפלטפורמה כשיש פריטים שנכשלו.
