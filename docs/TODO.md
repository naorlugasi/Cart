# TODO - מה עדיין פתוח (נכון ל-9.9.2026)

סדר העדיפויות לפרודקשן וההחלטות העסקיות: [LAUNCH.md](LAUNCH.md).

## מה כבר עובד

- **13 רשתות** עם adapter מאומת מול האתר החי לעגלת אורח ומוצרים ארוזים: שופרסל, רמי לוי, קרפור, יוחננוף, חצי חינם,
  ויקטורי, טיב טעם, יינות ביתן, מחסני השוק, קשת טעמים, קוויק, שוק העיר, אקספרס מהדרין (`recon/e2e-<chain>.json`, `recon/selfpoint-live.json`).
- **ערוץ לקוח בלי תוסף**: סימניית "טען עגלה" (`/bookmarklet`) - הנתונים בכתובת, התוצאה חוזרת ב-`postMessage` ובפיקסל תמונה כש-fetch חסום
  (רמי לוי). מאומת בכל הרשתות. התוסף (`extension/`) הוא כלי פיתוח בלבד.
- **קטלוגים אמיתיים**: 4,000 מוצרים לפי ברקוד (נמכרים ב-3 רשתות לפחות) ומחירי **סניפי האונליין** של 9 רשתות
  (`npm run prices:fetch` / `prices:online` / `products:build`, פירוט ב-README). רשת ללא נתונים אמיתיים לא מוצגת בהשוואה.
- הפריסה: https://cart-transfer-redirect.vercel.app - Vercel מחובר ל-GitHub; כל push לענף `claude/cart-transfer-redirect-mvp-wyxm2l` נפרס אוטומטית (פירוט ב-docs/DEPLOY.md).

## 1. מוצרים שקילים (ירקות, פירות, בשר, מעדנייה) - הפער הכי גדול ללקוח

כל ה-adapters שולחים כמות ביחידות בלבד. פריט שקיל יידחה או ייכנס כ-"N יחידות", תלוי ברשת. בקטלוג האמיתי יש ~650 מוצרים בקטגוריית ירקות ופירות.

- [ ] פלטפורמה: להעביר ב-payload של ה-handoff את `isWeighted` ו-`unit` לכל פריט (הקטלוג כבר מכיל אותם; `payloadFor` ב-`src/handoff/handoffService.js` מסנן אותם).
- [ ] injector: לחשוף את השדות כמשתני תבנית (`isWeighted`, `unit`, `sellingMethod`).
- [ ] שופרסל: `sellingMethod: "BY_WEIGHT"` וכמות בק"ג לפי ה-step של הפריט (בהקלטה: `data-selling-method`, `data-step`, `max-quantity`).
- [ ] רמי לוי: אובייקט המוצר מ-`/api/catalog` מכיל `prop.by_kilo` / `sw_shakil` ו-`multiplication` (צעד, למשל 0.5); ה-`amount` ב-localStorage הוא המשקל.
- [ ] רשתות Self Point (קרפור וכו'): `soldBy` בשורת העגלה, `isWeighable` / `unitResolution` במוצר - להקליט הוספה של פריט שקיל.
- [ ] יוחננוף וחצי חינם (`IsShakil`): להקליט הוספה של פריט שקיל.
- [ ] לאמת עם `node scripts/e2e-handoff.mjs <chain>` אחרי הוספת פריט שקיל ל-`REAL_ITEMS`.

הערכה: יום-יומיים.

## 2. קטלוגים - השלמות

- [ ] **ויקטורי ומחסני השוק**: laibcatalog.co.il מחזיר "אין קבצים להצגה" לכל חיפוש (גם לרשת כולה, גם עם תאריכים קודמים) - לבדוק שוב בימים הקרובים / לפנות לרשת. בינתיים מחוץ להשוואה.
- [ ] **שוק העיר ואקספרס מהדרין**: לא נמצא פורטל שקיפות מחירים (הכתובות prices.*.co.il לא קיימות). אפשרות: מחירים מה-API של האתר (Self Point, `/v2/retailers/<id>/branches/<b>/products`) דרך דפדפן אמיתי, כי Cloudflare חוסם אוטומציה.
- [ ] **קשת טעמים**: סניף האונליין (ממ"ר) לא מפרסם קובץ; משתמשים בסניף 014. לבדוק אם ה-API של האתר (Self Point) נותן מחירי אונליין.
- [ ] **רמי לוי**: הקובץ הוא של סניף 005; המחירים מוחלפים במחירי האונליין מה-API (סניף 331) ומוצר שה-API לא מחזיר מסומן "לא במלאי". לוודא שזה נכון גם למוצרים שהאתר מציג רק לאחר בחירת אזור משלוח.
- [ ] **שופרסל**: חלק מהפריטים המיובאים עם ברקוד שאינו 729 נדחים ע"י האתר (הקוד באתר שונה); לבנות id-map מדפי המוצר ולסמן פריטים שנדחו כדי שה-UI יציג "יתווסף ידנית".
- [ ] **קטגוריות**: 605 מוצרים ב"כללי" - לשפר את חוקי המילים ב-`scripts/build-products.mjs`, או לקחת קטגוריות מה-API של יוחננוף/חצי חינם (יש להם עץ קטגוריות).
- [ ] **שמות**: הקבצים מכילים שמות קצוצים ("חלב תנובה 3% שומן 2"); להעדיף שמות מה-API של האתרים כשיש.
- [ ] **מבצעים**: קובצי PromoFull מיובאים אבל רק חלק מהמבצעים מתורגמים לחוקי תמחור (`promoRuleFromFields`); לבדוק כיסוי מול מבצעי "2 ב-", "השני ב-50%" וכו'.
- [ ] **רענון יומי**: להריץ את הצינור (fetch → online → build) ולפרוס אוטומטית (GitHub Action עם `vercel deploy`; שימו לב שחלק מהפורטלים חוסמים כתובות ענן - ייתכן שצריך runner עצמי).
- [ ] `GET /api/mapping/stats` איטי מאוד עם 4,000 מוצרים (התאמה מטושטשת לכל מוצר לא ממופה) - להגביל או לחשב מראש.

## 3. סניף / אזור משלוח

כל אתר משתמש בסניף ברירת המחדל שלו (קרפור 3003 כפר סבא, רמי לוי 331, יוחננוף "צומת חולון") עד שהמשתמש בוחר אחר, וגם המחירים
בקטלוג הם של סניף אונליין אחד לכל רשת. נתוני המשלוח של 9 הרשתות החדשות ב-`data/chains.json` (דמי משלוח, מינימום, ערים) הם ברירת מחדל.

- [ ] להעביר את הסניף שנבחר בפלטפורמה (`branchId` ב-handoff) ל-`vars` של ה-adapter במקום ברירת המחדל.
- [ ] למפות בין הסניפים ב-`data/chains.json` למזהי הסניפים של האתרים, ולהשלים דמי משלוח / מינימום / ערים אמיתיים.
- [ ] רשתות Self Point: פריט שאינו פעיל בסניף נשמט בשקט ע"י האתר ומדווח אצלנו כ-rejected (נראה באקספרס מהדרין); לשקול סינון `branch.isActive` ב-lookup והצעת סניף אחר.

## 4. רמי לוי - משתמש מחובר

ה-adapter ממלא את עגלת האורח ב-localStorage. למשתמש מחובר יש עגלת שרת (`EcomToken`) והסל שלנו לא יופיע בה.

- [ ] להקליט את קריאות העגלה של משתמש מחובר (דורש חשבון אמיתי; לא ניתן לבצע מהסביבה האוטומטית).
- [ ] לזהות מצב מחובר (`localStorage.ramilevy.authuser.user`) ולבחור אסטרטגיה בהתאם.

## 5. ערוץ ה-bookmarklet ומובייל

- [x] הסימנייה מכילה את ה-injector; כשהוא משתנה צריך לגרור אותה מחדש. נעשה: לכל build יש גרסה (hash), הדיאלוג זוכר איזו גרסה נגררה ומבקש לגרור מחדש כשהיא השתנתה, מציג אזהרה כשאין דיווח אחרי 25 שניות, ומזהה דיווח שהגיע מגרסה ישנה.
- [ ] ה-payload בכתובת אינו חתום (רק ה-`cart_id`). מי שילחץ על הסימנייה בכתובת מזויפת יוסיף לעגלה שלו את מה שהכתובת מכילה; לשקול חתימה ואימות בסימנייה עם מפתח ציבורי.
- [ ] מובייל (Safari iOS / Chrome Android) בלי שורת סימניות נוחה: לבדוק את ה-WebView (`mobile/`) על מכשיר אמיתי מול רשת אמיתית; הסקריפט כבר כולל את ה-payload.
- [ ] להריץ פעם אחת את הזרימה המלאה עם הסימנייה בכרום של משתמש אמיתי ברשתות שמאחורי Cloudflare (קרפור, ויקטורי, מחסני השוק, קשת טעמים, יינות ביתן, קוויק, שוק העיר, אקספרס מהדרין) ולשמור את הדיווח ב-`recon/`.

## 6. ממצאים מהבדיקה הידנית (8.9.2026, ערב)

- [ ] **סטטוס ב-Vercel**: `GET /api/handoffs/:id/status` מחזיר "ממתין" כשהדיווח נקלט ב-instance אחר (הפונקציות stateless). ה-UI מקבל את
  התוצאה מהטאב של הרשת ב-`postMessage`, אז ללקוח זה עובד, אבל לניטור/התראות צריך אחסון משותף (Redis/KV) ל-`recordResults`.
- [ ] **יוחננוף**: `AddProductsToCart` מוסיף לכמות הקיימת (לא idempotent). הרצה חוזרת של אותה סימנייה בטאב חדש תכפיל כמויות; לשקול `updateCartItems` כשהפריט כבר בעגלה.
- [ ] **יינות ביתן**: במבה קלאסי קיימת בקובץ המחירים של סניף 472 אבל לא פעילה באתר (`branch.isActive=false`); ה-lookup מסנן עכשיו לפי זמינות ומדווח "לא זמין באתר הרשת". כדאי להעביר את הדגל הזה לקטלוג (מוצר לא זמין אונליין) בזמן הייבוא.
- [ ] **קוויק**: תוקן - האתר רץ על www.quik.co.il; ה-injector פונה עכשיו תמיד לדומיין של הדף.
- [x] בדיקה ידנית עברה ב-9 הרשתות שבהשוואה (שופרסל, רמי לוי, קרפור, יוחננוף, קוויק, קשת טעמים, יינות ביתן, טיב טעם, חצי חינם) עם מוצרים אמיתיים מהקטלוג.

## 7. הצגת העגלה אחרי הטעינה

- [x] Self Point (קרפור, קוויק, טיב טעם, קשת, ביתן...): פאנל העגלה נפתח בטעינה (`frontend.cartClosed="0"`).
- [x] חצי חינם: הפניה ל-`/checkout` (דף הקופה, שלב "עגלה"). שופרסל: `/online/he/cart` (לאורח - דף התחברות שממשיך לעגלה). רמי לוי: העגלה גלויה בצד בדף הבית.
- [ ] יוחננוף: אין דף עגלה (מגירה בלבד, `/cart` ו-`/checkout` הם 404); הלקוח לוחץ על כפתור העגלה. לבדוק אם אפשר לפתוח את המגירה דרך פרמטר או אירוע.
- [x] **הסימנייה עצמה**: הקוד היה נשבר בשמירה כסימנייה (דפדפנים מוחקים מעברי שורה מ-URL והערות `//` בלעו את הקוד). תוקן ע"י קידוד URL; מי שגרר את הסימנייה לפני 8.9.2026 בערב צריך למחוק ולגרור מחדש.

## 8. חוסן לאורך זמן

- [ ] הטוקן האנונימי של רמי לוי (`ANONYMOUS_TOKEN` ב-`src/handoff/adapters/ramilevy.js`) מגיע מה-bundle של האתר ועלול להתחלף; להוסיף בדיקה תקופתית שמזהה 401 ומרימה התראה.
- [ ] `scripts/probe-adapters.js` בודק רק `OPTIONS` על נתיב ה-add; לעדכן אותו למבנה החדש (lookup / session) ולהריץ מ-GitHub Actions (מכתובות ענן חלק מהאתרים חוסמים).
- [ ] הודעות ה-banner באתר הרשת: להוסיף קישור חזרה לפלטפורמה כשיש פריטים שנכשלו.
- [ ] ה-UI: הסל נשמר ב-localStorage; אחרי רענון קטלוג מוצרים שנמחקו מוסרים מהסל (נעשה), אבל שינויי מחיר לא מוצגים ללקוח - להציג "המחיר התעדכן".
