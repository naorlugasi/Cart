# TODO - מה עדיין פתוח (נכון ל-9.9.2026)

סדר העדיפויות לפרודקשן וההחלטות העסקיות: [LAUNCH.md](LAUNCH.md).

## מה כבר עובד

- **13 רשתות** עם adapter מאומת מול האתר החי לעגלת אורח ומוצרים ארוזים: שופרסל, רמי לוי, קרפור, יוחננוף, חצי חינם,
  ויקטורי, טיב טעם, יינות ביתן, מחסני השוק, קשת טעמים, קוויק, שוק העיר, אקספרס מהדרין (`recon/e2e-<chain>.json`, `recon/selfpoint-live.json`).
- **ערוץ לקוח בלי תוסף**: סימניית "טען עגלה" (`/bookmarklet`) - הנתונים בכתובת, התוצאה חוזרת ב-`postMessage` ובפיקסל תמונה כש-fetch חסום
  (רמי לוי). מאומת בכל הרשתות. התוסף (`extension/`) הוא כלי פיתוח בלבד.
- **קטלוגים אמיתיים**: 4,000 מוצרים לפי ברקוד (נמכרים ב-3 רשתות לפחות) ומחירי **סניפי האונליין** של 9 רשתות
  (`npm run prices:fetch` / `prices:online` / `products:build`, פירוט ב-README). רשת ללא נתונים אמיתיים לא מוצגת בהשוואה.
- הפריסה: https://cart-transfer-redirect.vercel.app - Vercel מחובר ל-GitHub; כל push לענף `claude/cart-transfer-redirect-mvp-wyxm2l` נפרס אוטומטית (פירוט ב-docs/DEPLOY.md).

## 1. מוצרים שקילים (ירקות, פירות, בשר, מעדנייה) - הפער הכי גדול ללקוח

כל ה-adapters שולחים כמות ביחידות בלבד. פריט שקיל יידחה או ייכנס כ-"N יחידות", תלוי ברשת. בקטלוג האמיתי יש ~650 מוצרים בקטגוריית ירקות ופירות.

- [ ] פלטפורמה: להעביר ב-payload של ה-handoff את `isWeighted` ו-`unit` לכל פריט (הקטלוג כבר מכיל אותם; `payloadFor` ב-`src/handoff/handoffService.js` מסנן אותם).
- [ ] injector: לחשוף את השדות כמשתני תבנית (`isWeighted`, `unit`, `sellingMethod`).
- [ ] שופרסל: `sellingMethod: "BY_WEIGHT"` וכמות בק"ג לפי ה-step של הפריט (בהקלטה: `data-selling-method`, `data-step`, `max-quantity`).
- [ ] רמי לוי: אובייקט המוצר מ-`/api/catalog` מכיל `prop.by_kilo` / `sw_shakil` ו-`multiplication` (צעד, למשל 0.5); ה-`amount` ב-localStorage הוא המשקל.
- [ ] רשתות Self Point (קרפור וכו'): `soldBy` בשורת העגלה, `isWeighable` / `unitResolution` במוצר - להקליט הוספה של פריט שקיל.
- [ ] יוחננוף וחצי חינם (`IsShakil`): להקליט הוספה של פריט שקיל.
- [ ] לאמת עם `node scripts/e2e-handoff.mjs <chain>` אחרי הוספת פריט שקיל ל-`REAL_ITEMS`.

הערכה: יום-יומיים.

## 2. קטלוגים - השלמות

- [x] **ויקטורי ומחסני השוק** (9.9.2026): הפורטל של Laib עבר ל-JSON API (`laibcatalog.co.il/<chain>/index.html` → `/webapi/api/getfiles`); הדרייבר נכתב מחדש בלי Playwright, שתי הרשתות בקטלוג עם סניף האינטרנט (097).
- [ ] **אקספרס מהדרין**: לא ברשימה הרשמית (מתחת לסף), אין קובצי מחירים. לפי הכלל (מחירים רק ממחירון מפורסם) נשארת עם העברה בלבד, מחוץ להשוואה. שוק העיר: מחוברת (18.9), ראו סעיף 2.
- [x] **אושר עד** נוספה (9.9.2026): מחירי הסניף הגדול (026), מסומנת "קנייה בסניף בלבד" בהשוואה כי אין לה אתר הזמנות.
- [x] **קשת טעמים**: ממ"ר 120 כן מפרסם, בשם קובץ בלי מקטע תת-רשת; תוקן הסינון (17.9). נשאר: overlay זמינות מ-Self Point.
- [x] **יוחננוף = תתי-רשת פיקאפ** (DATA-SERVICE-PLAN §4.1.1): `yochananof` (A, 18 נקודות, קובץ 050) ו-`yochananof_b` (B, בת ים/נס ציונה, קובץ 015) בהשוואה, מסומנות "איסוף עצמי בלבד". C (צומת חולון, רמלה, יד אליהו) לא מוצגת: אין מחירון מפורסם.
- [ ] **יוחננוף C**: פנייה לרשת על המחירון הלא-מפורסם של צומת חולון / רמלה / יד אליהו. **טיוטה מוכנה: [letters/yochananof-pricelist-c.md](letters/yochananof-pricelist-c.md)**, נאור שולח. כשיתפרסם קובץ: שורה ב-SOURCES (`yochananof_c`) והיא נכנסת כתת-רשת שלישית.
- [x] **שוק העיר**: דרייבר Bina מחובר (18.9). בקטלוג: חנות מקוונת 305, זהה למחירון שהאתר גובה בכל הסניפים שנבדקו (5 סניפים, 100%). **לא** מפוצלת לתתי-רשת: הקבצים האזוריים 304/309/319 (זולים יותר) לא נגבים באף סניף באתר, ולכן הצגתם הייתה מבטיחה מחיר שאי אפשר לקבל. מסומנת "איסוף עצמי בלבד" (משלוח לא זמין, נבדק 18.9). נשאר: overlay אימות אוטומטי מ-Self Point (דפדפן אמיתי).
- [ ] **תמונות מוצר**: לא מאתרי הרשתות (הוחלט 18.9: שום פנייה לאתרים). אפשרויות: מאגר תמונות לפי ברקוד (Open Food Facts, ספק מסחרי), או תמונות מהרשתות בהסכמה כשיהיה הסכם. עד אז אין תמונות.
- [ ] **handoff יוחננוף**: ההעברה מוסיפה לעגלה ב-view ברירת המחדל (s82 = מחירון C). ה-adapter צריך לקבוע את ה-view של תת-הרשת שנבחרה (s116 / s84) כדי שהמחיר בקופה יהיה המחיר שהוצג. `yochananof_b` רשומה עם `verified: false` עד אז.
- [ ] **UI**: להציג ליד כל מחיר את תאריך המחירון והחנות המקוונת שממנה נבנה (`source.store`, `generatedAt`).
- [x] **רמי לוי**: נמצא קובץ החנות המקוונת (039, `pricefull…-039-…`, ZIP); הוא המקור, 98% זהה לאתר. ה-API רק מאמת ומסמן מלאי (17.9).
- [x] **שופרסל, קודי המוצר**: הוחלט (18.9) שהקטלוג נבנה רק ממה שהרשת מפרסמת, ולכן קוד ההעברה נגזר **מהנוסחה בלבד** (`P_<ברקוד>`, ולברקודי 729000 `P_<המספר אחרי הקידומת>`). המפענח מול חיפוש האתר (`npm run audit:shufersal-codes`) הוא כלי ביקורת ידני בלבד, לא חלק מהצינור; בביקורת של 18.9: 2,924/2,925 תואמים לנוסחה, 12 מוצרים שבקובץ אינם באתר.
- [ ] **שופרסל, קוד המוצר באתר** (רקע): הנוסחה (`P_<ברקוד>`, ולברקודי 729000 `P_<המספר אחרי הקידומת>`) אומתה ב-17.9 על 65 מוצרים מדגמיים (40 ברקוד מלא, 25 קצר) מול חיפוש האתר: 65/65 זהים. כדי שזה יחזיק גם אם שופרסל תשנה קודים, להוסיף לצינור היומי **מפענח קבוע**: `GET /online/he/search/results?q=<ברקוד>:relevance` (JSON, בלי דפדפן, ~3,000 קריאות, קצב 5/שנייה) שקובע את `storeItemId` בקטלוג מהאתר עצמו, עם הנוסחה כ-fallback, ומסמן מוצר שהאתר לא מכיר כ"יתווסף ידנית". הדחיות שנראו בעבר כנראה היו מוצרים שאינם נמכרים אונליין ולא קוד שגוי; למדוד שוב אחרי המפענח.
- [ ] **קטגוריות**: 605 מוצרים ב"כללי" - לשפר את חוקי המילים ב-`scripts/build-products.mjs`, או לקחת קטגוריות מה-API של יוחננוף/חצי חינם (יש להם עץ קטגוריות).
- [ ] **שמות**: הקבצים מכילים שמות קצוצים ("חלב תנובה 3% שומן 2"); להעדיף שמות מה-API של האתרים כשיש.
- [x] **מבצעים**: הפרסר מכסה את כל 14 הרשתות (19.9, 98-100% מהמבצעים הרלוונטיים; `docs/PROMOS-AND-PRIVATE-LABEL.md` §0). פתוח: מבצעי מגוון בין ברקודים, מבצעים בצינור הסניפים, UI.
- [x] **מושגים, מותג פרטי בקטלוג, תחליפים** (19.9): `docs/CONCEPTS.md`, `docs/FRONTEND-BACKEND-UPDATES.md`. פתוח: כיסוי מושגים ל-85% (היום 73%), שמות מותגי בית לטיב טעם/קשת/מחסני השוק/שוק העיר, הגדרת הלקוח בפרופיל.
- [x] **רענון יומי** (17.9.2026): המק של נאור מריץ את הצינור ב-06:00 וב-12:00 ודוחף את הקטלוגים לענף ([RUNNER-MAC.md](RUNNER-MAC.md)). מה שנשאר מ-[DATA-SERVICE-PLAN.md](DATA-SERVICE-PLAN.md): R2/DuckDB וכל הסניפים (שלבים 1-4).
- [ ] `GET /api/mapping/stats` איטי מאוד עם 4,000 מוצרים (התאמה מטושטשת לכל מוצר לא ממופה) - להגביל או לחשב מראש.

## 3. סניף / אזור משלוח

כל אתר משתמש בסניף ברירת המחדל שלו (קרפור 3003 כפר סבא, רמי לוי 331, יוחננוף "צומת חולון") עד שהמשתמש בוחר אחר, וגם המחירים
בקטלוג הם של סניף אונליין אחד לכל רשת. נתוני המשלוח של 9 הרשתות החדשות ב-`data/chains.json` (דמי משלוח, מינימום, ערים) הם ברירת מחדל.

- [ ] להעביר את הסניף שנבחר בפלטפורמה (`branchId` ב-handoff) ל-`vars` של ה-adapter במקום ברירת המחדל.
- [ ] למפות בין הסניפים ב-`data/chains.json` למזהי הסניפים של האתרים, ולהשלים דמי משלוח / מינימום / ערים אמיתיים.
- [ ] רשתות Self Point: פריט שאינו פעיל בסניף נשמט בשקט ע"י האתר ומדווח אצלנו כ-rejected (נראה באקספרס מהדרין); לשקול סינון `branch.isActive` ב-lookup והצעת סניף אחר.

## 4. רמי לוי - משתמש מחובר

ה-adapter ממלא את עגלת האורח ב-localStorage. למשתמש מחובר יש עגלת שרת (`EcomToken`) והסל שלנו לא יופיע בה.

- [ ] **הוחלט לבנות (18.9).** להקליט את קריאות העגלה של משתמש מחובר: נאור מתחבר לרמי לוי בכרום שלו, מקליטים את ההוספה (צפוי: `POST /api/v2/cart` עם Bearer `EcomToken` מ-localStorage, אותו endpoint שמשמש היום לתמחור עגלת אורח עם הטוקן האנונימי).
- [ ] לזהות מצב מחובר (`localStorage.ramilevy.authuser.user` / `EcomToken`) ולבחור אסטרטגיה: מחובר → קריאות API עם הטוקן של המשתמש; אורח → localStorage כמו היום.
- [ ] בדיקה ידנית משני המצבים ושמירת ההקלטה ב-`recon/ramilevy-loggedin.json`.

## 5. ערוץ ה-bookmarklet ומובייל

- [x] הסימנייה מכילה את ה-injector; כשהוא משתנה צריך לגרור אותה מחדש. נעשה: לכל build יש גרסה (hash), הדיאלוג זוכר איזו גרסה נגררה ומבקש לגרור מחדש כשהיא השתנתה, מציג אזהרה כשאין דיווח אחרי 25 שניות, ומזהה דיווח שהגיע מגרסה ישנה.
- [ ] ה-payload בכתובת אינו חתום (רק ה-`cart_id`). מי שילחץ על הסימנייה בכתובת מזויפת יוסיף לעגלה שלו את מה שהכתובת מכילה; לשקול חתימה ואימות בסימנייה עם מפתח ציבורי.
- [ ] מובייל (Safari iOS / Chrome Android) בלי שורת סימניות נוחה: לבדוק את ה-WebView (`mobile/`) על מכשיר אמיתי מול רשת אמיתית; הסקריפט כבר כולל את ה-payload.
- [ ] להריץ פעם אחת את הזרימה המלאה עם הסימנייה בכרום של משתמש אמיתי ברשתות שמאחורי Cloudflare (קרפור, ויקטורי, מחסני השוק, קשת טעמים, יינות ביתן, קוויק, שוק העיר, אקספרס מהדרין) ולשמור את הדיווח ב-`recon/`.

## 6. ממצאים מהבדיקה הידנית (8.9.2026, ערב)

- [ ] **סטטוס ב-Vercel**: `GET /api/handoffs/:id/status` מחזיר "ממתין" כשהדיווח נקלט ב-instance אחר (הפונקציות stateless). ה-UI מקבל את
  התוצאה מהטאב של הרשת ב-`postMessage`, אז ללקוח זה עובד, אבל לניטור/התראות צריך אחסון משותף (Redis/KV) ל-`recordResults`.
- [ ] **יוחננוף**: `AddProductsToCart` מוסיף לכמות הקיימת (לא idempotent). הרצה חוזרת של אותה סימנייה בטאב חדש תכפיל כמויות; לשקול `updateCartItems` כשהפריט כבר בעגלה.
- [ ] **יינות ביתן**: במבה קלאסי קיימת בקובץ המחירים של סניף 472 אבל לא פעילה באתר (`branch.isActive=false`); ה-lookup מסנן עכשיו לפי זמינות ומדווח "לא זמין באתר הרשת". כדאי להעביר את הדגל הזה לקטלוג (מוצר לא זמין אונליין) בזמן הייבוא.
- [ ] **קוויק**: תוקן - האתר רץ על www.quik.co.il; ה-injector פונה עכשיו תמיד לדומיין של הדף.
- [x] בדיקה ידנית עברה ב-9 הרשתות שבהשוואה (שופרסל, רמי לוי, קרפור, יוחננוף, קוויק, קשת טעמים, יינות ביתן, טיב טעם, חצי חינם) עם מוצרים אמיתיים מהקטלוג.

## 7. הצגת העגלה אחרי הטעינה

- [x] Self Point (קרפור, קוויק, טיב טעם, קשת, ביתן...): פאנל העגלה נפתח בטעינה (`frontend.cartClosed="0"`).
- [x] חצי חינם: הפניה ל-`/checkout` (דף הקופה, שלב "עגלה"). שופרסל: `/online/he/cart` (לאורח - דף התחברות שממשיך לעגלה). רמי לוי: העגלה גלויה בצד בדף הבית.
- [ ] יוחננוף: אין דף עגלה (מגירה בלבד, `/cart` ו-`/checkout` הם 404); הלקוח לוחץ על כפתור העגלה. לבדוק אם אפשר לפתוח את המגירה דרך פרמטר או אירוע.
- [x] **הסימנייה עצמה**: הקוד היה נשבר בשמירה כסימנייה (דפדפנים מוחקים מעברי שורה מ-URL והערות `//` בלעו את הקוד). תוקן ע"י קידוד URL; מי שגרר את הסימנייה לפני 8.9.2026 בערב צריך למחוק ולגרור מחדש.

## 8. חוסן לאורך זמן

- [ ] הטוקן האנונימי של רמי לוי (`ANONYMOUS_TOKEN` ב-`src/handoff/adapters/ramilevy.js`) מגיע מה-bundle של האתר ועלול להתחלף; להוסיף בדיקה תקופתית שמזהה 401 ומרימה התראה.
- [ ] `scripts/probe-adapters.js` בודק רק `OPTIONS` על נתיב ה-add; לעדכן אותו למבנה החדש (lookup / session) ולהריץ מ-GitHub Actions (מכתובות ענן חלק מהאתרים חוסמים).
- [ ] הודעות ה-banner באתר הרשת: להוסיף קישור חזרה לפלטפורמה כשיש פריטים שנכשלו.
- [ ] ה-UI: הסל נשמר ב-localStorage; אחרי רענון קטלוג מוצרים שנמחקו מוסרים מהסל (נעשה), אבל שינויי מחיר לא מוצגים ללקוח - להציג "המחיר התעדכן".

## 8א. החלטת 18.9: שום פנייה לאתרי הרשתות

- [x] חוזה נתונים לצד השרת: [PIPELINE-CONTRACT.md](PIPELINE-CONTRACT.md) (18.9).

- [x] הריצה היומית פונה רק לפורטלי שקיפות המחירים. `prices:online` ו-`prices:codes` הוסרו מ-daily-refresh והפכו לכלי ביקורת ידניים (`audit:online-check`, `audit:shufersal-codes`). הקטלוג לא מקבל מהאתרים מחיר, מוצר, מלאי, תמונה או קוד.
- [ ] מלאי אונליין: היום כל מוצר שבקובץ מוצג כזמין; מוצר שהאתר לא מוכר יתגלה רק בהעברה ("לא נוסף"). לשקול לבקש מהרשתות שדה זמינות בקובץ, או להסתפק בדיווח ההעברה.

## 10. המנוע של הסימנייה - החלטות 18.9 (נבנה)

סקירה טכנית של "טען עגלה" עם נאור, 18.9.2026. מה שנבדק ולא דורש פעולה, ומה שהוחלט לבנות.

- [x] **אורך הכתובת - נבדק, אין בעיה.** 46KB הם בכתובת של הסימנייה עצמה (אומת בכרום עם הסימנייה האמיתית, 46,086 בייט). בכתובת של אתר הרשת נוסע רק ה-`#`: ~3KB מתכון + ~60 בייט לפריט (80 פריטים ≈ 8KB מול מגבלה של 2MB, ולא נשלח לשרת הרשת). "למשוך את הסל ב-fetch מהשרת שלנו" נוסה ונפסל: ה-CSP של רמי לוי חוסם `connect-src` לכל דומיין זר.
- [ ] **מיניפיקציה אמיתית** של ה-injector בסימנייה (terser במקום ניקוי הערות): 46KB → ~18-20KB. משנה את הגרסה, לכן פעם אחת כל המשתמשים יתבקשו לגרור מחדש; לעשות יחד עם שינוי מנוע אחר.
- [ ] **ניטור פסיבי (הדיווחים החוזרים).** אחסון משותף ל-`recordResults` (Vercel KV / Upstash Redis) במקום זיכרון של instance. התראה כשרשת עוברת 50% כישלונות ב-20 ההעברות האחרונות, וסימון אוטומטי "באימות" באתר (כפתור ההזמנה מוחלף ב"רשימה להעתקה"). זה סעיף 1-2 ב-LAUNCH.md.
- [ ] **ניטור אקטיבי.** ריצה יומית של סל בדיקה (3 מוצרים) מול כל רשת בדפדפן אמיתי, מהמק של נאור (Cloudflare חוסם אוטומציה מענן ב-8 רשתות). **דורש החלטה מול 8א**: זו פנייה לאתרי הרשתות, אם כי בהיקף של לקוח אחד ביום. עד להחלטה: הרצה ידנית של `scripts/e2e-handoff.mjs --manual` לפני כל פריסה שנוגעת ב-adapters.
- [ ] **זיהוי כבוט.** הקריאות יוצאות מהדפדפן של הלקוח עם העוגיות שלו, אבל 14 הוספות ב-3 שניות נראות כמו סקריפט. להעלות את ההשהיה בין פריטים ל-400ms, להעדיף הוספה מרוכזת איפה שהרשת תומכת (קרפור וכל Self Point כבר), ולרשום 403/429 בדיווח כסוג כישלון נפרד כדי לראות את זה מוקדם.
- [ ] רמי לוי למשתמש מחובר: סעיף 4.

## 11. תנאי שימוש, מותג אנונימי וגבייה - נדחה (18.9)

הוחלט 18.9 להכניס לרשימה ולטפל מאוחר יותר. הבסיס: "אפשרות 2" - מודל מסחרי (מנוי ₪10, PRODUCT 5.2) תחת מותג אנונימי, כך שהרשתות והמשתמשים רואים רק את המותג ולא את נאור.

- [ ] **תנאי שימוש ודיסקליימר** בדף הרכישה/ההזמנה: המשתמש מסמן אישור; התוכנה מספקת אוטומציה מקומית בדפדפן של הלקוח בלבד, המותג הוא "צינור", הלקוח פוטר את המותג מנזק וטענות צד ג'. יחד עם מדיניות פרטיות, הצהרת נגישות וגילוי שממלאים עגלה בשם המשתמש (LAUNCH סעיף 7, PRODUCT סעיף 7: ביטול מנוי קל, הודעה לפני חיוב, נתוני תשלום אצל הסולק בלבד).
- [ ] **שם מותג** - טרם נבחר.
- [ ] **תשתית דיסקרטית**: תיבת מייל ייעודית לפרויקט (ProtonMail), דומיין בינלאומי (.com/.io) עם WHOIS Privacy (Namecheap נותנת בחינם), חשבונות Vercel/Supabase/Render על המייל של הפרויקט ולא על חשבון גוגל אישי.
- [ ] **גבייה**: אין סליקה אנונימית (KYC: ת.ז., אישור ניהול חשבון, עוסק). הפתרון: עוסק מורשה/פטור על השם האמיתי, סולק ישראלי (קארדקום / משולם) עם דף תשלום וחשבונית בשם המותג בלבד (white label). מי שיודע את השם האמיתי: הסולק ומס הכנסה; חשיפה לרשת דורשת צו בית משפט מול הסולק.
- [ ] **שאלה פתוחה**: אנונימיות מתנגשת עם ההחלטה ב-LAUNCH שההסכם הראשון עם רשת שווה יותר מהכנסה מיידית (הסכם דורש זהות), ועם מסלול האפליקציה (חנויות אפל/גוגל דורשות מפתח מזוהה). להחליט לפני שרוכשים דומיין.

## 9. פניות לרשתות

- [ ] **שוק העיר, קבצים 304/309/319** (נאור שולח; קובץ מלא עם נספח: [letters/shukcity-regional-pricelists.md](letters/shukcity-regional-pricelists.md)). עד לתשובה לא מוצגים. הנוסח:

> **לכבוד:** ט.ע.מ.ס שוק העיר בע"מ, מחלקת דיגיטל / שירות לקוחות
> **מאת:** ____________, ____________ (סל חכם)
> **תאריך:** 18.9.2026
>
> **הנדון: בירור לגבי המחירונים המקוונים המתפרסמים בפורטל שקיפות המחירים (חנויות 304, 309, 319)**
>
> שלום רב,
>
> אנו מפעילים שירות השוואת מחירים לצרכנים המבוסס אך ורק על קובצי המחירים שרשתות המזון מפרסמות לפי תקנות קידום התחרות בענף המזון (שקיפות מחירים), תשע"ה-2014. בפורטל הפרסום של הרשת (shuk-hayir.binaprojects.com) מצאנו, נכון ל-17-18.9.2026, שבע חנויות המסווגות בקובץ הסניפים כחנויות מקוונות (StoreType 2) עם כתובת אתר האינטרנט של הרשת: 304 "אונליין - רמות", 305 "אונליין - אשקלון צפוני", 309 "אונליין - קרית גת", 311 "אונליין - כפר סבא", 312 "אונליין - בני ברק", 313 "אונליין - רמלה", 319 "אונליין - אור ים".
>
> בהשוואה למחירים המוצגים באתר הרשת (shukcity.co.il) בסניפים קרית גת, כפר סבא, רמות, בני ברק ורמלה מצאנו:
> 1. המחירים באתר, בכל הסניפים שנבדקו, **זהים** למחירון המתפרסם בחנויות 305, 311, 312 ו-313 (המחירונים של ארבע החנויות הללו זהים זה לזה).
> 2. המחירונים המתפרסמים בחנויות **304, 309 ו-319** שונים מהמחירים באתר בכל הסניפים שנבדקו, כולל בסניף "רמות" מול חנות 304 "אונליין - רמות" ובסניף "קרית גת" מול חנות 309 "אונליין - קרית גת" (התאמה של 7 מתוך 20 ו-13 מתוך 44 מוצרים שנבדקו, בהתאמה). במרבית המוצרים שבהם יש הבדל, המחיר במחירונים אלה **נמוך** מהמחיר באתר.
>
> **נבקש להבהיר:**
> 1. לאיזה ערוץ מכירה שייכים המחירונים של חנויות 304, 309 ו-319 (אתר, אפליקציה, הזמנה טלפונית, איסוף עצמי מסניף מסוים), וכיצד לקוח יכול לרכוש במחירים אלה.
> 2. אם לקוח אינו יכול לרכוש במחירים אלה, מהו הבסיס לפרסומם כחנויות מקוונות פעילות בקובץ הסניפים.
> 3. כיצד ניתן לזהות בקבצים המפורסמים את המחירון החל על לקוח לפי אזור המשלוח או נקודת האיסוף שבחר באתר.
>
> המידע ישמש אותנו להצגה נכונה של מחירי הרשת לצרכנים. נשמח למענה תוך 14 יום. הנתונים המלאים (קבצי המקור והשוואות) זמינים לפי בקשה.
>
> בכבוד רב,
> ____________

- [ ] **יוחננוף, מחירון C** (נאור שולח; קובץ מלא עם נספח האימות: [letters/yochananof-pricelist-c.md](letters/yochananof-pricelist-c.md)). הנוסח:

> **לכבוד:** מ. יוחננוף ובניו (1988) בע"מ, מחלקת דיגיטל / מחלקה משפטית
> **עותק:** משרד הכלכלה והתעשייה, הממונה על יישום תקנות שקיפות המחירים
> **מאת:** ____________, ____________ (סל חכם)
> **תאריך:** 18.9.2026
>
> **הנדון: אי-פרסום מחירון לחנות המקוונת (נקודות איסוף צומת חולון, רמלה ותל אביב יד אליהו) לפי תקנות קידום התחרות בענף המזון (שקיפות מחירים), תשע"ה-2014**
>
> שלום רב,
>
> אנו מפעילים שירות השוואת מחירים לצרכנים המבוסס אך ורק על קובצי המחירים שרשתות המזון מפרסמות לפי תקנות שקיפות המחירים. בבדיקה שערכנו ב-17.9.2026 בפורטל הפרסום של הרשת (url.publishedprices.co.il, משתמש `yohananof`) מצאנו את הפערים הבאים, ונבקש את תיקונם:
>
> **1. חנות מקוונת רשומה ללא מחירון.**
> קובץ הסניפים שפרסמה הרשת (`Stores7290803800003-000-20260917-010000.xml`) כולל את חנות מס' **150 "הדרים פיקאפ"** בסיווג **StoreType 2 (חנות מקוונת)**. לחנות זו לא פורסם אף קובץ PriceFull, Price, PromoFull או Promo, בעוד שלכל 47 הסניפים הפיזיים (StoreType 1) מתפרסמים קבצים מלאים מדי יום ועדכונים מדי שעה.
>
> **2. מחירון האתר לנקודות האיסוף צומת חולון, רמלה ויד אליהו אינו מופיע באף קובץ מפורסם.**
> אתר הרשת (yochananof.co.il) מוכר לאיסוף עצמי מ-23 נקודות איסוף. בדקנו את המחירים המוצגים באתר לכל נקודת איסוף מול כל 47 קובצי הסניפים:
> - ב-20 נקודות איסוף מחירי האתר **זהים ב-100%** לקובץ הסניף המשרת אותן (18 נקודות תואמות למחירון של קבוצת סניפים הכוללת את סניף 001/050, ושתיים, בת ים ונס ציונה, למחירון של קבוצת סניף 015).
> - בשלוש נקודות איסוף, **צומת חולון בן צבי** (נקודת האיסוף המוגדרת כברירת מחדל באתר), **רמלה רמ"א** ו**תל אביב יד אליהו**, מחירי האתר זהים זה לזה (512 מתוך 512 מוצרים שנבדקו) אך **אינם תואמים לאף קובץ מחירים שפורסם** (התאמה מרבית לסניף כלשהו: 76%-77%).
> - במחירון זה, בכל מוצר שבו קיים הבדל לעומת המחירון של 18 נקודות האיסוף האחרות (108 מתוך 486 מוצרים שנבדקו), המחיר **גבוה יותר**, ללא חריג.
>
> לפי התקנות, קמעונאי גדול מפרסם מחירון מלא לכל חנות, וחנות מקוונת נכללת בהגדרה זו (סיווג 2 בשדה "סוג חנות" בנספח לתקנות). מאחר שהמחירים הנגבים בפועל מלקוחות המזמינים לאיסוף בצומת חולון, רמלה ויד אליהו אינם מתפרסמים, הצרכן אינו יכול להשוותם, וזו בדיוק התכלית שהתקנות באו להשיג.
>
> **נבקש:**
> 1. לפרסם, במתכונת הקיימת (PriceFull, Price, PromoFull, Promo), את מחירון החנות/החנויות המשרתות את נקודות האיסוף צומת חולון, רמלה ויד אליהו, או להבהיר איזה מזהה סניף בקבצים המפורסמים מייצג אותן.
> 2. לפרסם מחירון לחנות 150 המסווגת כמקוונת, או לעדכן את סיווגה בקובץ הסניפים.
> 3. להבהיר כיצד לזהות בקבצים המפורסמים את המחירון החל על כל אחת מ-23 נקודות האיסוף (למשל באמצעות שדה מתאים בקובץ הסניפים), כדי שהצרכן יוכל לדעת מראש איזה מחיר ישלם באיסוף.
>
> נשמח לקבל מענה תוך 14 יום. הנתונים המלאים (קבצי המקור, השוואות ורשימת המוצרים) זמינים לפי בקשה. אין באמור כדי לגרוע מכל זכות.
>
> בכבוד רב,
> ____________
